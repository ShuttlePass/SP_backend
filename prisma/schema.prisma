generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model classes {
  cl_idx      Int       @id @default(autoincrement())
  company_idx Int
  cl_name     String    @db.VarChar(255)
  cl_start_at DateTime  @db.Time(0)
  cl_end_at   DateTime  @db.Time(0)
  created_at  DateTime? @default(now()) @db.Timestamp(0)
  updated_at  DateTime? @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model classes_day {
  cd_idx      Int                @id @default(autoincrement())
  classes_idx Int
  cd_day      classes_day_cd_day
  created_at  DateTime?          @default(now()) @db.Timestamp(0)
  updated_at  DateTime?          @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model classes_enrollment {
  ce_idx      Int       @id @default(autoincrement())
  student_idx Int
  classes_idx Int
  ce_date     DateTime  @db.Date
  created_at  DateTime? @default(now()) @db.Timestamp(0)
  updated_at  DateTime? @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model company {
  co_idx     Int       @id @default(autoincrement())
  co_name    String    @unique(map: "co_name") @db.VarChar(255)
  created_at DateTime? @default(now()) @db.Timestamp(0)
  updated_at DateTime? @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model course_reservation {
  cr_idx          Int       @id @default(autoincrement())
  course_idx      Int
  reservation_idx Int
  cr_seq          Int?
  created_at      DateTime? @default(now()) @db.Timestamp(0)
  updated_at      DateTime? @default(now()) @db.Timestamp(0)

  @@unique([course_idx, cr_seq], map: "course_reservation_index_0")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model reservation_enrollment {
  re_idx          Int       @id @default(autoincrement())
  reservation_idx Int
  enrollment_idx  Int
  created_at      DateTime? @default(now()) @db.Timestamp(0)
  updated_at      DateTime? @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model shuttle {
  sh_idx     Int       @id @default(autoincrement())
  sh_name    String    @db.VarChar(255)
  driver_idx Int
  sh_state   Int       @db.TinyInt
  sh_max_cnt Int
  created_at DateTime? @default(now()) @db.Timestamp(0)
  updated_at DateTime? @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model shuttle_area {
  sa_idx      Int       @id @default(autoincrement())
  shuttle_idx Int
  area_idx    Int
  created_at  DateTime? @default(now()) @db.Timestamp(0)
  updated_at  DateTime? @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model shuttle_course {
  sc_idx       Int       @id @default(autoincrement())
  shuttle_idx  Int
  sc_arrive_at DateTime  @db.DateTime(0)
  created_at   DateTime? @default(now()) @db.Timestamp(0)
  updated_at   DateTime? @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model shuttle_reservation {
  sr_idx           Int       @id @default(autoincrement())
  shuttle_idx      Int
  shuttle_time_idx Int
  student_idx      Int
  sr_address       String    @db.VarChar(255)
  sr_address_memo  String?   @db.VarChar(255)
  sr_address_x     Decimal   @db.Decimal(9, 6)
  sr_address_y     Decimal   @db.Decimal(9, 6)
  sr_state         Int       @db.TinyInt
  created_at       DateTime? @default(now()) @db.Timestamp(0)
  updated_at       DateTime? @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model shuttle_time {
  st_idx      Int       @id @default(autoincrement())
  st_time     DateTime  @db.Time(0)
  company_idx Int
  created_at  DateTime? @default(now()) @db.Timestamp(0)
  updated_at  DateTime? @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model student {
  st_idx       Int       @id @default(autoincrement())
  company_idx  Int
  area_idx     Int
  st_name      String    @db.VarChar(255)
  st_contact   String    @db.VarChar(255)
  st_address   String    @db.VarChar(255)
  st_address_x Decimal   @db.Decimal(9, 6)
  st_address_y Decimal   @db.Decimal(9, 6)
  created_at   DateTime? @default(now()) @db.Timestamp(0)
  updated_at   DateTime? @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model user {
  us_idx      Int            @id @default(autoincrement())
  us_id       String         @unique(map: "us_id") @db.VarChar(255)
  us_password String         @db.VarChar(255)
  us_level    user_us_level?
  company_idx Int
  us_contact  String         @db.VarChar(255)
  us_name     String         @db.VarChar(255)
  created_at  DateTime?      @default(now()) @db.Timestamp(0)
  updated_at  DateTime?      @default(now()) @db.Timestamp(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model area {
  ar_idx      Int       @id @default(autoincrement())
  company_idx Int
  ar_name     String    @db.VarChar(255)
  created_at  DateTime? @default(now()) @db.Timestamp(0)
  updated_at  DateTime? @default(now()) @db.Timestamp(0)
}

enum classes_day_cd_day {
  MON
  TUE
  WED
  THU
  FRI
  SAT
  SUN
}

enum user_us_level {
  manager
  driver
}
